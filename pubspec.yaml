name: tool_schema_generator
description: Generate JSON Schema Draft 2020-12 tool definitions and dispatchers for LLM function calling from annotated Dart functions.
version: 0.3.0
repository: https://github.com/youssefdawoud-dev/tool_schema_generator
homepage: https://github.com/youssefdawoud-dev/tool_schema_generator

environment:
  sdk: ^3.9.0

dependencies:
  analyzer: '>=8.1.1 <14.0.0'
  build: '>=3.0.0 <5.0.0'
  source_gen: ^4.2.3

dev_dependencies:
  build_runner: ^2.4.0
  build_test: ^3.0.0
  test: ^1.31.1
